# Contributing

感谢你愿意改进 Pixel Asset Forge。本项目目标是保持轻量、可复现、易演示，优先使用原生 Web 能力实现功能。

## 本地开发

1. Fork 或 clone 仓库。
2. 直接打开 `index.html`，或运行静态服务器：

```bash
python -m http.server 4173
```

3. 访问 `http://localhost:4173/`。
4. 修改 `index.html`、`styles.css`、`app.js` 后刷新浏览器验证。

## 代码原则

- 不引入构建工具，除非能明显提升可维护性并写清楚原因。
- 新增第三方依赖前，请在 PR 描述和 README 中说明用途、许可证和替代方案。
- 保持导出结果可复现：同一提示词、参数和 seed 应生成同一结果。
- UI 文案使用中文，代码命名使用清晰英文。
- 兼顾桌面和移动端布局，避免按钮、文本、画布互相遮挡。

## PR 规则

- 每个 PR 只做一件事。
- PR 描述需要包含功能描述、实现思路和测试方式。
- 合并前请至少验证：
  - `node --check app.js`
  - 浏览器打开页面无控制台报错
  - 修改提示词和参数后画布、动作帧、元数据同步更新
  - PNG、精灵表、JSON 导出按钮可用

## 适合贡献的方向

- 新增素材类型或提示词关键词。
- 改进像素生成质量和风格一致性。
- 增加 Unity / Godot / Aseprite 辅助导出。
- 增加批量生成、ZIP 打包或可编辑像素网格。
- 完善文档、Demo 脚本和测试清单。
